% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/S3plot.R
\name{plot}
\alias{plot}
\alias{plot.MIRT}
\alias{plot.MGPCM}
\alias{plot.MGGUM}
\alias{plot.FCMIRT}
\alias{plot.FCDCM}
\alias{plot.FCGGUM}
\alias{plot.FCGDINA}
\alias{plot.TIRT}
\title{S3 Methods: plot}
\usage{
\method{plot}{MIRT}(x, y = NULL, ...)

\method{plot}{MGPCM}(x, y = NULL, ...)

\method{plot}{MGGUM}(x, y = NULL, ...)

\method{plot}{FCMIRT}(x, y = NULL, ...)

\method{plot}{FCDCM}(x, y = NULL, ...)

\method{plot}{FCGGUM}(x, y = NULL, ...)

\method{plot}{FCGDINA}(x, y = NULL, ...)

\method{plot}{TIRT}(x, y = NULL, ...)
}
\arguments{
\item{x}{A fitted model object of class \code{"MIRT"},
\code{"MGPCM"}, \code{"MGGUM"}, \code{"FCMIRT"}, \code{"FCDCM"},
\code{"FCGGUM"}, \code{"FCGDINA"}, or \code{"TIRT"}.}

\item{y}{Ignored.}

\item{...}{Ignored.}
}
\value{
Invisibly returns \code{NULL}. Plots are produced as a side
effect on the current graphics device.
}
\description{
Draws log-likelihood trace plots for fitted \pkg{ForceChoice} model
objects. Stan, iStEM, and EM fits are visualised through the same
convergence target: the total log-likelihood recorded across iterations
of the corresponding estimation algorithm.
}
\details{
The horizontal axis is the saved Stan iteration, iStEM iteration, or EM
iteration. The vertical axis is the summed log-likelihood. Stan plots use
\code{log_lik} or \code{log_lik_group} from generated quantities and draw
all chains in one panel. iStEM plots use the iteration-level
\code{iStEM$logLik.trace}, and the stable chain is the final
\code{M * B} iStEM iterations. EM plots use \code{EM$logLik.trace}. For
Stan and iStEM fits, a vertical dashed line marks the stable-chain
boundary. EM fits do not have a stable-chain boundary.
}
\section{Methods (by class)}{
\itemize{
\item \code{plot(MIRT)}: Log-likelihood trace plot for \code{MIRT} objects.

\item \code{plot(MGPCM)}: Log-likelihood trace plot for \code{MGPCM} objects.

\item \code{plot(MGGUM)}: Log-likelihood trace plot for \code{MGGUM} objects.

\item \code{plot(FCMIRT)}: Log-likelihood trace plot for \code{FCMIRT} objects.

\item \code{plot(FCDCM)}: Log-likelihood trace plot for \code{FCDCM} objects.

\item \code{plot(FCGGUM)}: Log-likelihood trace plot for \code{FCGGUM} objects.

\item \code{plot(FCGDINA)}: Log-likelihood trace plot for \code{FCGDINA} objects.

\item \code{plot(TIRT)}: Log-likelihood trace plot for \code{TIRT} objects.

}}
