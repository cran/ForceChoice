% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.data.TIRT.R
\name{sim.data.TIRT}
\alias{sim.data.TIRT}
\title{Simulate Data from the Thurstonian IRT (TIRT) Model}
\usage{
sim.data.TIRT(
  N.person = 1000,
  N.block = 10,
  I.block = 2,
  D = 3,
  fc.type = "RANK",
  control = NULL
)
}
\arguments{
\item{N.person}{Integer; number of persons (default: 1000).}

\item{N.block}{Integer; number of blocks (default: 10).}

\item{I.block}{Integer; items per block (default: 2).}

\item{D}{Integer; number of latent dimensions (default: 3, >= 2).}

\item{fc.type}{Character; \code{"RANK"} (default), \code{"MOLE"}, or
\code{"PICK"}.}

\item{control}{Optional list with entries \code{Q.matrix}, \code{block.items},
\code{lambda}, \code{psi2}, \code{gamma.matrix}, \code{gamma.item},
\code{Corr}.}
}
\value{
An object of class \code{"data.TIRT"} containing \code{data},
\code{response}, \code{theta}, \code{par}, \code{Q.matrix},
\code{block.items}, \code{Corr}, \code{gamma.matrix}, \code{lambda},
\code{psi2}, \code{pairs.value}, and data-generating arguments.
}
\description{
Generates forced-choice pairwise comparison data under the Thurstonian
IRT framework. Each statement loads on exactly one latent dimension;
pairwise latent differences with probit link determine binary outcomes.
Supports RANK, MOLE, and PICK response types. For each pair \eqn{(i,k)},
the simulated comparison uses
\eqn{-\gamma_{ik} + \lambda_i q_i'\theta_j -
\lambda_k q_k'\theta_j + \epsilon_i - \epsilon_k}, with
\eqn{\epsilon_i \sim N(0,\psi_i^2)}.
}
\examples{
set.seed(123)
sim <- sim.data.TIRT(N.person = 20, N.block = 3, I.block = 2,
                     D = 2, fc.type = "RANK")
str(sim$data)
head(sim$response)
dim(sim$Q.matrix)

}
\seealso{
\code{\link{fit.TIRT}}, \code{\link{model.TIRT}}
}
