#! /bin/sh

# Generated by rstantools, with package-local skip guard.

if grep -iq '^Config/ForceChoice/skip-rstantools-config:[[:space:]]*true' DESCRIPTION; then
  exit 0
fi

"${R_HOME}/bin/Rscript" -e "rstantools::rstan_config()"
